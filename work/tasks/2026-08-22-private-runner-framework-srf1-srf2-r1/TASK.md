# TASK — Private Runner Framework SRF1/SRF2 R1

Implement and validate the reusable source contracts that private consumer repositories require before repository-bound Mac mini Runner activation.

## Deliverables

- strict private-runner schemas;
- stdlib semantic validator;
- positive synthetic fixture and negative matrix;
- private repository workflow starter template;
- repository-bound service contract;
- host mutex/protected-resource contract;
- rollback/credential-revocation contract;
- hosted framework CI.

## Boundaries

`chatgpt-parent-pm` is public and must not register a Self-hosted Runner. No consumer product source, private paths, credentials, D2/D3 data, Provider, merge, tag or release is in scope.
