# Local Deployment Agent Role

The deployment agent reconstructs a GitHub candidate in a real local environment. It is intentionally narrow.

Its success criterion is not “the code looks correct”; it is “the exact candidate was built and started with reproducible evidence.”
